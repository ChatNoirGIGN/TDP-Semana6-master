class Car {
  final int id;
  final String color;
  final String marca; //aka modelo xd
  final String placa;
  final double latitude;
  final double longitude;
  Car(this.id, this.color, this.marca, this.placa, this.latitude, this.longitude);
}